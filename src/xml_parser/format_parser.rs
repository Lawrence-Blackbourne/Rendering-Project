#[derive(Copy, Clone, Debug, PartialEq, Eq)]
pub(super) enum FormatXMLElement {
    RegularFormat(ParsedRegularFormat),
}

#[derive(Copy, Clone, Debug, PartialEq, Eq)]
pub(super) enum ParsedRegularFormat {
    
}